{% set name = "geomeTRIC" %}
{% set version = "0.9.8a0" %}
{% set sha256 = "7df0f5e3c886febc927fb57923b990d3afa5903fc6e3dce73e230759e9546752" %}
{% set build = 1 %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/robertodr/{{ name }}/archive/vlx.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: {{ build }}
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv"
  entry_points:
    - geometric-optimize = geometric.optimize:main

requirements:
  host:
    - pip
    - python
    - setuptools
  run:
    - {{ pin_compatible('numpy', lower_bound='1.11') }}
    - networkx
    - python
    - six

test:
  imports:
    - geometric
  requires:
    - pytest
  commands:
    - $PYTHON -m pytest --pyargs geometric -v -k json

about:
  home: http://github.com/leeping/geomeTRIC/
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'Molecular geometry optimizer in the TRIC coordinate system.'
  dev_url: http://github.com/leeping/geomeTRIC/

extra:
  recipe-maintainers:
    - robertodr
    - leeping
    - dgasmith
    - yudongqiu
